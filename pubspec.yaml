name: update_helper
description: Help you easier to control the update dialog. It also supports the feature that forces the app to update to continue using.
version: 0.0.2
homepage: https://github.com/vursin/update_helper

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter
  in_app_review: ^2.0.4
  package_info_plus: ^1.4.3+1
  universal_platform: ^1.0.0+1
  url_launcher: ^6.1.5
  satisfied_version: ^0.0.1+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter: